[package]
name = "data-analysis-with-rust-book"
version = "0.1.0"
edition = "2024"

[lib]
name = "data_analysis_with_rust_book"

[[bin]]
name = "data-analysis-with-rust-book"
path = "src/bin/main.rs"

[[example]]
name = "etl_only"
path = "examples/etl_only.rs"

[[example]]
name = "summary_demo"
path = "examples/summary_demo.rs"

[dependencies]
anyhow = "1.0.86"
aws-sdk-s3 = { version = "1.111.0", features = ["behavior-version-latest"] }
clap = { version = "4.5.51", features = ["string", "derive"] }
# comrak = "0.47.0"
connectorx = { path = "connector-x-patched/connectorx", features = ["src_postgres", "dst_polars"] }

# Convert data from one version of Polars to another version of Polars
df-interchange = { version = "0.3.1", features = [
  "polars_0_49",
  "polars_0_50",
  "polars_0_51",
] }

# hypors = "0.3.0"
# plotlars = { version = "0.10.4", features = [
#   "static_export_geckodriver",
#   "static_export_wd_download",
# ] }
polars = { version = "0.52.0", features = [
  # "abs",          # Absolute value
  "aws",   # Read/write cloud (minio)
  "cloud",
  "csv",   # Read/write CSVs
  # "cum_agg",      # Cumulative aggregate statistics
  # "fmt",          # Format tables as markdown
  "is_in",   # List filter
  "lazy",    # LazyFrame
  "parquet", # Parquet files
  # "pivot",        # Pivot data
  # "regex",        # Call columns with regex
  # "replace",      # Replace value
  # "round_series", # Round values
  "timezones", # Needed as per https://github.com/pola-rs/polars/issues/25148#issuecomment-3505173352
] }
rand = "0.9.2"
rand_chacha = "0.9.0"
# rust_xlsxwriter = "0.92.0"
sqlx = { version = "0.8.6", features = ["postgres", "runtime-tokio"] }
tokio = { version = "1.48.0", features = ["rt", "tracing"] }
# zip = "6.0.0"
reqwest = { version = "0.12.24", features = ["rustls-tls", "json"] }
tracing = "0.1.41"
tracing-subscriber = { version = "0.3.20", features = ["env-filter"] }
async-trait = "0.1.89"

